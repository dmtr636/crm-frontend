import {observer} from "mobx-react";
import styled from "styled-components";

const Container = styled.div`
	position: absolute;
	top: 102px;
	right: 48px;
	height: 100px;
    width: 208px;
    background: #FFFFFF;
    border-radius: 0px 0px 5px 5px;
`

export const HeaderMemberMenu = observer(() => {
	return (
		<Container>

		</Container>
	)
})